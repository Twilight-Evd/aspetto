import 'package:flutter/material.dart';

const Size minWindowSize = Size(1200, 720);
const Size minSplashWindowSize = Size(600, 360);

const double headerHeight = 50;
const double leftWidth = 262;
const double mainSpace = 3;
